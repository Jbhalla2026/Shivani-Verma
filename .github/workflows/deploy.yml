The yarn.lock file is missing from the repo, causing the cache setup to fail. You need to update the workflow file.

Go to https://github.com/Jbhalla2026/Shivani-Verma/blob/main/.github/workflows/deploy.yml

Click the pencil icon (edit) and replace the entire content with this:

name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: cd frontend && yarn install

      - name: Build
        run: cd frontend && GENERATE_SOURCEMAP=false yarn build
        env:
          REACT_APP_BACKEND_URL: ""

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/build

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
